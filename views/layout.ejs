<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>SpeakEasy: your penpal app</title>
    <link rel="stylesheet" href="/css/normalize.css" media="screen" title="no title" charset="utf-8">
    <link rel="stylesheet" href="/css/master.css" media="screen" title="no title" charset="utf-8">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.2.0/jquery.min.js"></script>
    <script src="../public/scripts/chat.js"></script>
  </head>
  <body>
    <nav>
      <li><a href='/'>home</a></li>
      <li><a href='/about'>about</a></li>
      <% if (currentUser) { %>
        <li><a href='/users'>users page</a></li>
       <li> Hello, <%= currentUser.userName %></li>
       <li><a href="/auth/logout">Logout</a></li>
      <% } else { %>
      <li><a href='/auth/signup'>create account</a></li>
      <li><a href='/auth/login'>login</a></li>
      <% }%>
    </nav>
    <% if(typeof alerts !== 'undefined'){ %>
      <% for(key in alerts){ %>
          <% alerts[key].forEach(function(thisMsg){ %>
              <div class="alert alert-<%= key %>"><%= thisMsg %></div>
        <% }); %>
      <% } %>
     <% } %>
    <%- body %>
    <footer><small>art party sustainable echo park meditation.</small></footer>
  </body>
</html>
